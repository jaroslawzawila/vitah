services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "4432:5432"
    environment:
      POSTGRES_DB: vitah
      POSTGRES_USER: vitah
      POSTGRES_PASSWORD: vitah_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
